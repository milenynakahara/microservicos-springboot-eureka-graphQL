type User{
    id: ID!,
    name: String,
    cpf: Int
}

input UserInput{
    name: String,
    cpf: Int
}

type Query{
     findAllUsers: [User],
     findUserById(id: Long): User,
     hello: String
 }
